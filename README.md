How to run both the API and the graphiql-explorer

## Setup

Install dependencies:

```
#in directory ./apiMockup AND in ./graphiql-with-explorer
npm install
```

Start two servers:

```
#in both directory ./apiMockup AND in ./graphiql-with-explorer
npm start
```